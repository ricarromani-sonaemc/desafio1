pipeline {
    agent any

    stages {
        stage("build")

        steps {
            echo 'build...'
        }
    }

    stages {
        stage("test")

        steps {
            echo 'test...'
        }
    }
    stages {
        stage("deploy")

        steps {
            echo 'deploy...'
        }
    }
}
